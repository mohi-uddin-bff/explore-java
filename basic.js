/** 3 way to run java-
        a) Browser (inspect / F2);
        b) VS code;
        c) online (codepen.io);

    *variable-
        short key of variable is var. There have some types of variable-
            1) Number; var bananaPrice = 80;
            2) String; var name = `Mohiuddin`; or var speech = `I am dracula....`;
            3) Boolean; var isLeapYear = false / true;

            to get output of theis variable-
                console.log(bananaPrice, name, isLeapYear);
        
        Here bananaPrice / isLeapYear we use upper case alphabet to start a new word. It's called camel case. */





    
/** some basic for java-

for upper case, lawer case, position & divide;
var name = `Every time we will Help You`
console.log(name.toLowerCase());
console.log(name.toUpperCase());
console.log(name.indexof(`we`));
console.log(name.split(`e`, ` `)); */ 


/** integer, float, & random number-

        let number = -2.458;
        let result = Math.ceil(number);
        let result2 = Math.floor(number);
        let result3 = Math.round(number);
        let result4 = Math.random() * 6;
        let result5 = Math.round(result4);
        let result6 = Math.abs(number)
        console.log(result, result2, result3, result5, result6); */


    
        
